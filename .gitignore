# Dependencies
node_modules/

# Build output
dist/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Temp
*.tmp
*.temp

# Playwright
test-results/
playwright-report/
playwright/.cache/

# Generated custom-elements-manifest + editor-integration artifacts.
# Regenerated by `npm run analyze` / `npm run build` and shipped via the
# package.json "files" field — kept out of git for the same reason as dist/
# (build output, derived from ATTRIBUTE_TABLE + DatePickerOptions JSDoc).
custom-elements.json
web-types.json
vscode.html-custom-data.json
vscode.css-custom-data.json

# npm pack output
*.tgz

# Per-developer Make configuration (container runner, image name/port overrides)
.makefile.env
